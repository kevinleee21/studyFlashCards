package Controller;

/**
 * The Options enum represents the difference options that the user
 * is able to select when going through the flashcard application
 */
public enum Options {
  HARD, EASY, SHOW, EXIT
}

